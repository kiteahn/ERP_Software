<Window x:Class="PhanMemInAnERP.Views.JournalPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xem trước bút toán kế toán" Height="520" Width="920"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="txtTitle" Grid.Row="0" FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,8"/>
        <TextBlock x:Name="txtHint" Grid.Row="1" Foreground="#555" TextWrapping="Wrap" Margin="0,0,0,8"/>
        <DataGrid x:Name="gridLines" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Dòng" Binding="{Binding LineNo}" Width="50"/>
                <DataGridTextColumn Header="TK" Binding="{Binding AccountCode}" Width="60"/>
                <DataGridTextColumn Header="Tên TK" Binding="{Binding AccountName}" Width="*"/>
                <DataGridTextColumn Header="Nợ (VNĐ)" Binding="{Binding DebitText}" Width="120"/>
                <DataGridTextColumn Header="Có (VNĐ)" Binding="{Binding CreditText}" Width="120"/>
                <DataGridTextColumn Header="Diễn giải" Binding="{Binding Explanation}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock x:Name="txtBalance" Grid.Row="3" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy" Width="100" Height="32" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button x:Name="btnConfirm" Content="Xác nhận ghi sổ" Width="140" Height="32" Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
